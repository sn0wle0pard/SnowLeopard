<% page.posts.each(function(post, i) { %>
    <a href="<%- url_for(post.path) %>"><%-post.title %></a>
<% }) %>
<% if (page.total > 1){ %>
    <nav id="page-nav">
        <%- paginator({
            prev_text: "__('prev')",
            next_text: "__('next')"
        }) %>
    </nav>
<% } %>
